name: turbo_response
description: A type-safe response wrapper for handling success and failure states with pattern matching and utility methods.
version: 1.0.1
repository: https://github.com/appboypov/turbo_response
homepage: https://github.com/appboypov/turbo_response
issue_tracker: https://github.com/appboypov/turbo_response/issues

topics:
  - error-handling
  - result
  - response
  - type-safe
  - dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.0
  lints: ^3.0.0
